apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: rent
  labels:
  {{- include "movies.labels" . | nindent 4 }}
spec:
  gateways:
  - mesh
  hosts:
  - rent.{{ .Release.Namespace }}.svc.cluster.local
  http:
  - route:
    - destination:
        host: rent.{{ .Release.Namespace }}.svc.cluster.local
        subset: stable

---

apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: rent
  labels:
  {{- include "movies.labels" . | nindent 4 }}
spec:
  host: rent.{{ .Release.Namespace }}.svc.cluster.local  
  subsets:
  - name: stable    
    labels:
      app.kubernetes.io/name: api